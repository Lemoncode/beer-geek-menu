// @ts-check

/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  sassOptions: {
    // includePaths: [path.join(__dirname, 'css')],
    includePaths: ['css'],
  },
};

export default nextConfig;
